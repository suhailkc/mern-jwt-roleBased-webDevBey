# My Express App

A simple Node.js + Express project with jwt auth with role based access routes

## How to run
1. run 'npm install'
2. create a .env file from .env.example and add values
3. run 'npm run dev'